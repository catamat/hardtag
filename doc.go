// Package hardtag is a simple package that generates a deterministic SHA-256 hardware fingerprint of the machine it runs on.
// It works on Linux, Windows, and macOS with two identification strategies — instance and hybrid — that cover both privileged and unprivileged execution contexts.
package hardtag
